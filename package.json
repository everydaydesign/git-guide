{
  "name": "git-guide",
  "version": "1.0.0",
  "private": true,
  "description": "Guide to Git — static site",
  "license": "MIT",
  "scripts": {
    "build:text": "node scripts/build-text.mjs",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "prepare": "husky"
  },
  "lint-staged": {
    "*.{css,js,json,md,html}": "prettier --write"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.2.11",
    "marked": "^18.0.5",
    "marked-gfm-heading-id": "^4.1.4",
    "prettier": "^3.4.2"
  }
}
